package functional_interfaces_and_lambda;

// An interface with just one abstract method is called Functional Interface.
// Since functional interface has just one function, you need not to mention that which
// function's definition you are giving.

public interface Test {
    void calculate(int a, int b);
}
